// Entrada
let numero = Number(prompt("Informe um número inteiro: "));
// Processamento
let anterior = numero - 1;
let posterior = numero + 1;
// Saída
alert(`Número: ${numero}, Anterior: ${anterior}, Posterior: ${posterior}`);
